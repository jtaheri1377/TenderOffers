import { MinOfObjectsPipe } from './min-of-objects.pipe';

describe('MinOfObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new MinOfObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
